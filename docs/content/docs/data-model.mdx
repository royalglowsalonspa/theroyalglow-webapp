---
title: Data Model
description: The 38-table Drizzle schema for Royal Glow — conventions, table groupings, enums, and ID formats.
---

# Data Model

Royal Glow runs on **Neon PostgreSQL 16** with **Drizzle ORM** (pure TypeScript,
edge-native). The schema lives in `packages/db/src/schema/` as **15 files**
defining **38 tables** that cover auth, profiles, services, scheduling,
bookings, billing, memberships, offers, CRM, leads, loyalty, notifications,
branches, and system operations.

<Callout type="info">
  Every monetary value is an **integer in paise** (₹1 = 100 paise) — never a
  float. Every timestamp is `timestamptz` (stored UTC, displayed IST). Prices
  are GST-inclusive at 18%.
</Callout>

## Conventions

These rules are mandatory across every table.

<TypeTable
  type={{
    'Primary keys': { type: 'text', description: 'App-generated via nanoid() (or cuid2()). No auto-increment serials — prevents enumeration attacks.' },
    Money: { type: 'integer (paise)', description: '₹1,000.00 = 100000. No floating point; round at display only.' },
    Timestamps: { type: 'timestamptz', description: 'Everywhere. Stored UTC, displayed IST (UTC+5:30).' },
    'Date display': { type: 'DD/MM/YYYY', description: "Via Intl.DateTimeFormat('en-IN')." },
    'Currency display': { type: '₹1,00,000.00', description: "Indian grouping via Intl.NumberFormat('en-IN'), always 2 decimals." },
    Naming: { type: 'snake_case', description: 'Singular table names (booking, not bookings).' },
    Enums: { type: 'native CREATE TYPE', description: 'Centralised in enums.ts.' },
    Deletes: { type: 'hard delete', description: 'No soft-delete columns. Tracked via audit_log.' },
    'Foreign keys': { type: 'explicit ON DELETE', description: 'CASCADE for children, RESTRICT for references. Indexed on all FK columns.' },
    Snapshots: { type: 'frozen copies', description: 'Price + service/staff name frozen on booking_service and invoice_item so historical records never drift.' },
    GST: { type: '18% inclusive', description: 'SAC 999721. Back-calc: base = round(price ÷ 1.18).' },
  }}
/>

## Schema files & table groupings

The 15 schema files in `packages/db/src/schema/` map to 38 tables. The tree below
lists the actual `pgTable` names defined in each file.

<Files>
  <Folder name="packages/db/src/schema" defaultOpen>
    <File name="enums.ts — all PostgreSQL enums (not a table)" />
    <File name="auth.ts — user, session, account, verification (4)" />
    <File name="profile.ts — customer_profile, staff_profile (2)" />
    <File name="service.ts — service_category, service, staff_service (3)" />
    <File name="schedule.ts — staff_schedule, staff_time_off, business_hour, holiday (4)" />
    <File name="booking.ts — booking, booking_service, booking_status_log, waitlist (4)" />
    <File name="invoice.ts — invoice, invoice_item (2)" />
    <File name="membership.ts — spa_membership_tier, spa_membership (2)" />
    <File name="offer.ts — offer, offer_service, offer_redemption (3)" />
    <File name="lead.ts — lead, lead_note (2)" />
    <File name="crm.ts — customer_tag, customer_tag_assignment, customer_note (3)" />
    <File name="loyalty.ts — loyalty_account, loyalty_transaction (2)" />
    <File name="notification.ts — notification, push_subscription (2)" />
    <File name="branch.ts — branch (1)" />
    <File name="system.ts — daily_sales_summary, monthly_gst_summary, audit_log, system_setting (4)" />
  </Folder>
</Files>

<Callout type="info">
  The `auth.ts` tables (`user`, `session`, `account`, `verification`) are owned
  by Better Auth's Drizzle adapter. Don't add custom columns to them — extend
  via `customer_profile` / `staff_profile` instead. Enums are defined separately
  in `enums.ts` (not counted as a table). Totals: **15 files**, **38 tables**.
</Callout>

## Key enums

Defined natively in PostgreSQL via `enums.ts`.

<TypeTable
  type={{
    booking_status: { type: 'enum', description: 'pending, confirmed, rejected, in_progress, completed, cancelled, no_show, rescheduled' },
    lead_status: { type: 'enum', description: 'new, contacted, follow_up, booked, won, lost' },
    payment_status: { type: 'enum', description: 'pending, paid, refunded' },
    payment_method: { type: 'enum', description: 'cash, upi, card, online' },
    invoice_type: { type: 'enum', description: 'service, membership_purchase, membership_session' },
    service_type: { type: 'enum', description: 'salon, spa' },
    discount_type: { type: 'enum', description: 'percentage, flat, combo_price' },
    spa_membership_status: { type: 'enum', description: 'active, expired, cancelled' },
    waitlist_status: { type: 'enum', description: 'waiting, notified, booked, expired, cancelled' },
    loyalty_tx_type: { type: 'enum', description: 'earned, redeemed, expired, adjusted' },
    leave_approval_status: { type: 'enum', description: 'pending, approved, rejected' },
    leave_type: { type: 'enum', description: 'sick, casual, personal, other' },
    staff_designation: { type: 'enum', description: 'receptionist, stylist, therapist, manager' },
    gender: { type: 'enum', description: 'male, female, other, prefer_not_to_say' },
    branch_status: { type: 'enum', description: 'operational, temporarily_closed, opens_soon, shutdown' },
    audit_action: { type: 'enum', description: 'create, update, delete, status_change' },
    notification_type: { type: 'enum', description: '24 values — reminder_24h, reminder_1h, booking_confirmed, booking_rescheduled, booking_cancelled, booking_rejected, membership_created, membership_session_recorded, membership_expiry_30d/7d/1d, membership_expired, membership_hours_low, membership_usage_nudge, birthday_offer, post_service_followup, leave_submitted/approved/rejected, lead_follow_up_due, stale_pending_booking, no_show_check, gems_expiry_7d, gems_expired' },
    notification_channel: { type: 'enum', description: 'push, email' },
    notification_status: { type: 'enum', description: 'pending, sent, failed' },
  }}
/>

## ID & number formats

| Entity | Format | Example |
| --- | --- | --- |
| Booking | `BK-{branch_code}-{YYMM}-{H\|S}-{5_random}[-M]` | `BK-RS-2605-H-38291` |
| Invoice | `INV-{branch_number}-{financial_year}-{5_random}` | `INV-1-2627-92921` |
| Membership | `RG-MEM-{YY}-{branch_number}-{5_random}` | `RG-MEM-26-1-90872` |

`H` = salon (hair/beauty), `S` = spa. The `-M` suffix marks a membership
session booking.

## GST (18% inclusive)

Customer-facing prices already include GST. The invoice back-calculates the base
and tax from the inclusive amount.

```ts
const GST_RATE = 0.18

function splitGST(inclusivePaise: number) {
  const basePaise = Math.round(inclusivePaise / (1 + GST_RATE))
  const gstPaise = inclusivePaise - basePaise
  return { basePaise, gstPaise, totalPaise: inclusivePaise }
}

// splitGST(118000) → { basePaise: 100000, gstPaise: 18000, totalPaise: 118000 }
// ₹1,180.00 inclusive = ₹1,000.00 base + ₹180.00 GST
```

## Loyalty (gems) rules

- **Earn:** 1 gem per ₹100 invoiced (floor), on `invoice_type = 'service'` only.
- **No gems** on `membership_purchase` or `membership_session` invoices.
- **Expiry:** 365 days from earn date, auto-deducted by the QStash gems-auto-expire job.
- **Redemption:** against specific catalogue services — not a ₹ discount.
- **Cannot combine** with an offer on the same booking.

## Booking lifecycle

```text
pending → confirmed/rejected → in_progress → completed
                                            ↘ cancelled    (from pending/confirmed)
                                            ↘ no_show       (from confirmed, +15min after end_time)
                                            ↘ rescheduled   (from confirmed)
```

Walk-ins skip `pending` and go straight to `confirmed`. Status transitions are
recorded in `booking_status_log`.

## Business rules enforced at the DB level

<Accordions type="single">

<Accordion title="One offer per customer per day">
Unique constraint on `offer_redemption` (customer + date).
</Accordion>

<Accordion title="One active membership per customer">
Partial unique index on `spa_membership` where `status = 'active'`.
</Accordion>

<Accordion title="One service type per booking">
A booking is salon OR spa, never mixed.
</Accordion>

<Accordion title="Snapshot pricing">
`booking_service.price_at_booking_paise` and `invoice_item.unit_price_paise` are frozen copies.
</Accordion>

</Accordions>

## Related pages

<Cards>
  <Card title="Conventions" href="/docs/conventions" description="Money, dates, layering, and the API response envelope" />
  <Card title="Background Jobs" href="/docs/background-jobs" description="The cron + QStash jobs that maintain this data" />
  <Card title="API Reference" href="/docs/api-reference" description="Endpoints that read and write these tables" />
</Cards>
