---
title: Environment Variables
description: Every environment variable for the Royal Glow platform — what they do, where they go, and how they're validated.
icon: sliders-horizontal
---

Royal Glow runs a monorepo with three Next.js apps (`apps/web`, `apps/admin`, `apps/cms`) plus the `apps/invoicing` PDF render service, deployed across Cloudflare Workers (`rgss-web`, `rgss-admin` — via the OpenNext adapter `@opennextjs/cloudflare`), Render (`rgss-cms`), and Cloud Run (`apps/invoicing`), and relies on ~15 external services.

<Warning>
  Environment variables are **validated at build time** using `@t3-oss/env-nextjs` + Zod. If any required variable is missing or malformed, the build fails immediately. Never use `process.env` directly — always import from `apps/web/src/env.ts`.
</Warning>

<Info>
  **Guarded-no-op pattern.** Optional integrations (Redis, R2, Resend, Brevo, web-push, Ably, Slack, QStash heartbeats) degrade gracefully. When their keys are absent the related call becomes a **no-op + log** instead of throwing: the health probe reports `skip` rather than `fail`, the Ably token route returns `503` so the client falls back to polling, and background jobs still complete and return `200`. This is why the app builds and runs with an incomplete `.env` — only the truly required core (database, auth) is hard-validated.
</Info>

## Quick Start

<Steps>
<Step title="Copy templates">
```bash
cp apps/web/.env.example apps/web/.env.local
cp apps/admin/.env.example apps/admin/.env.local
cp apps/cms/.env.example apps/cms/.env.local
```

</Step>
<Step title="Fill in values">
Populate each `.env.local` with values from your service dashboards.

</Step>
<Step title="Generate VAPID keys (one-time only)">
```bash
bunx web-push generate-vapid-keys
```

</Step>
<Step title="Validate">
The build fails fast if anything required is missing or malformed.

```bash
cd apps/web && bun run build
```

</Step>
</Steps>

## File Structure

<Tree>
  <Tree.File name=".env.example — root template (committed)" />
  <Tree.File name=".env.local — local dev secrets, NEVER commit" />
  <Tree.Folder name="apps/web" defaultOpen>
    <Tree.File name=".env.example — web-specific vars (committed)" />
    <Tree.File name=".env.local — local overrides (gitignored)" />
  </Tree.Folder>
  <Tree.Folder name="apps/admin" defaultOpen>
    <Tree.File name=".env.example — admin-specific vars (committed)" />
  </Tree.Folder>
  <Tree.Folder name="apps/cms" defaultOpen>
    <Tree.File name=".env.example — cms-specific vars (committed)" />
  </Tree.Folder>
</Tree>

## Complete Variable Reference

### Database (Neon)

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `DATABASE_URL` | `Secret · web, cms` | Yes | Pooled connection via Neon pgBouncer — used by all app queries. |
| `DATABASE_URL_UNPOOLED` | `Secret · web` | Yes | Direct connection — for migrations only. |

<Info>
  **Per-environment branches:** Use GitHub Environments so `DATABASE_URL` points to the correct Neon branch per environment (`prod` / `pprd` / `test` / `dev`).
</Info>

### Auth — Better Auth

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `BETTER_AUTH_SECRET` | `Secret` | Yes | Random string min 32 chars — signs sessions and tokens. |
| `BETTER_AUTH_URL` | `string` | Yes | Public app origin: https://theroyalglow.in |
| `GOOGLE_OAUTH_CLIENT_ID` | `Secret` | Yes | Google OAuth 2.0 client ID. |
| `GOOGLE_OAUTH_CLIENT_SECRET` | `Secret` | Yes | Google OAuth 2.0 client secret. |

### Email — Transactional (Resend)

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `RESEND_API_KEY` | `Secret` | Yes | Starts with re_ — transactional emails (invoices, confirmations). |
| `RESEND_FROM_EMAIL` | `string` | Yes | Default From header: `Royal Glow <contact@theroyalglow.in>` |

### Email — Marketing (Brevo)

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `BREVO_API_KEY` | `Secret` | Yes | Bulk/marketing email sends (offers, birthday, re-engagement). |

### Realtime — Ably

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `NEXT_PUBLIC_ABLY_KEY` | `Public` | Yes | Publishable key — sent to browser for client-side subscriptions. |
| `ABLY_PRIVATE_KEY` | `Secret` | Yes | Server-only key — used in API routes for publishing events. |

### File Storage — Cloudflare R2

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `R2_ACCOUNT_ID` | `Secret` | Yes | Cloudflare account ID. |
| `R2_ACCESS_KEY_ID` | `Secret` | Yes | R2 S3-compatible access key. |
| `R2_SECRET_ACCESS_KEY` | `Secret` | Yes | R2 S3-compatible secret key. |
| `R2_BUCKET_NAME` | `string` | Yes | Bucket name: theroyalglow-uploads |
| `NEXT_PUBLIC_R2_PUBLIC_URL` | `Public` | Yes | Public CDN URL: https://uploads.theroyalglow.in |

### Cache — Upstash Redis

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `UPSTASH_REDIS_REST_URL` | `Secret` | Yes | Upstash Redis REST endpoint. |
| `UPSTASH_REDIS_REST_TOKEN` | `Secret` | Yes | Upstash Redis auth token. |

### Queue — Upstash QStash

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `QSTASH_TOKEN` | `Secret` | Yes | Publishing token — used to enqueue jobs. |
| `QSTASH_CURRENT_SIGNING_KEY` | `Secret` | Yes | Verifies incoming QStash callbacks — current key. |
| `QSTASH_NEXT_SIGNING_KEY` | `Secret` | Yes | Verifies incoming QStash callbacks — rotated key. |

### Web Push — VAPID Keys

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `NEXT_PUBLIC_VAPID_PUBLIC_KEY` | `Public` | Yes | Sent to browser to create push subscription. |
| `VAPID_PRIVATE_KEY` | `Secret` | Yes | Used server-side to send push notifications. |
| `VAPID_SUBJECT` | `string` | Yes | mailto:contact@theroyalglow.in |

<Info>
  Generate once: `bunx web-push generate-vapid-keys`. These never change unless you intentionally rotate (which invalidates all existing push subscriptions).
</Info>

### Cloudflare — KV + CI/CD

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `CLOUDFLARE_ACCOUNT_ID` | `Secret` | Yes | Required for wrangler deploy and KV REST API writes. |
| `CLOUDFLARE_API_TOKEN` | `Secret` | Yes | Token with Workers + KV permissions. |
| `CLOUDFLARE_KV_NAMESPACE_ID` | `string` | Yes | KV namespace ID for edge-cached service listings. |

### Observability — Sentry

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `NEXT_PUBLIC_SENTRY_DSN` | `Public` | Yes | Sentry DSN — browser-side error capture. |
| `SENTRY_AUTH_TOKEN` | `Secret` | Yes | CI/CD only — uploads source maps during build. |
| `SENTRY_ORG` | `string` | Yes | Your Sentry organisation slug. |
| `SENTRY_PROJECT` | `string` | Yes | Your Sentry project slug: rgss |

### Observability — BetterStack

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `BETTER_STACK_TOKEN` | `Secret` | Yes | Log drain source token. |
| `BETTER_STACK_HEARTBEAT_NIGHTLY_SALES` | `string` | No | Heartbeat for nightly sales/GST/offer/gems jobs. |
| `BETTER_STACK_HEARTBEAT_PPRD_SYNC` | `string` | No | GitHub Actions: pprd DB sync. |
| `BETTER_STACK_HEARTBEAT_REMINDERS` | `string` | No | QStash: appointment reminder scheduler. |
| `BETTER_STACK_HEARTBEAT_MEMBERSHIP_EXPIRY` | `string` | No | QStash: membership auto-expire + expiry alerts. |
| `BETTER_STACK_HEARTBEAT_SESSION_CLEANUP` | `string` | No | QStash: session cleanup. |
| `BETTER_STACK_HEARTBEAT_BACKUP` | `string` | No | GitHub Actions: weekly R2 backup. |
| `BETTER_STACK_DEPLOY_WEBHOOK` | `string` | No | Deployment marker webhook. |
| `BETTER_STACK_INCIDENT_WEBHOOK` | `string` | No | Incident webhook on deploy/backup failure. |

### Analytics — PostHog & Clarity

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `NEXT_PUBLIC_POSTHOG_KEY` | `Public` | Yes | PostHog project API key: phc_xxx |
| `NEXT_PUBLIC_POSTHOG_HOST` | `Public` | Yes | https://us.i.posthog.com |
| `NEXT_PUBLIC_CLARITY_ID` | `Public` | Yes | Microsoft Clarity project ID. |

### Ads & Tracking — Meta

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `NEXT_PUBLIC_META_PIXEL_ID` | `Public` | Yes | Meta Pixel ID. |
| `META_PIXEL_ACCESS_TOKEN` | `Secret` | Yes | Conversions API (CAPI) access token. |
| `META_CAPI_WEBHOOK_TOKEN` | `Secret` | Yes | Verifies incoming Meta webhook payloads. |
| `META_TEST_EVENT_CODE` | `Secret · dev/pprd only` | No | Routes CAPI events to test panel — omit in production. |

### Reporting

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `SLACK_WEBHOOK_URL` | `Secret` | No | Slack incoming webhook — daily/weekly sales reports. |
| `DAILY_REPORT_EMAIL_RECIPIENTS` | `string` | No | Comma-separated emails for reports. |

### CRM — AiSensy

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `AISENSY_API_KEY` | `Secret` | No | AiSensy API key for WhatsApp integration. |
| `AISENSY_WEBHOOK_SECRET` | `Secret` | No | Verifies incoming AiSensy webhook payloads. |

### CMS — Payload

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `PAYLOAD_SECRET` | `Secret · cms` | Yes |  | Random secret used by Payload to encrypt tokens and cookies. |
| `SERVICE_SYNC_ENABLED` | `Private · cms` | No | `true` | Gates the service-catalogue sync hooks that mirror cms.* writes into public.*. Default enabled — only the literal string "false" disables it. Set to false while seeding, and as the primary rollback lever. |

### App Configuration

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `APP_ENV` | `Private` | Yes | `dev` | Distinguishes the 4 deployment environments: dev / test / pprd / prod. |
| `NEXT_PUBLIC_APP_URL` | `Public` | Yes | `http://localhost:3000` | Prod: https://theroyalglow.in (dev: http://localhost:3000). |
| `NEXT_PUBLIC_ADMIN_URL` | `Public` | No | `http://localhost:3001` | Admin portal origin — Prod: https://admin.theroyalglow.in (dev: http://localhost:3001). |

<Info>
  **`NODE_ENV` vs `APP_ENV`:** `NODE_ENV` is always `development` or `production` (Next.js convention). `APP_ENV` distinguishes between our 4 deployment environments: `dev`, `test`, `pprd`, `prod`. Use `APP_ENV` for environment-specific logic.
</Info>

## Variable Count Summary

| Category | Count |
| --- | --- |
| Database | 2 |
| Auth | 4 |
| Email (transactional + marketing) | 3 |
| Realtime (Ably) | 2 |
| File Storage (R2) | 5 |
| Cache (Redis) | 2 |
| Queue (QStash) | 3 |
| Web Push (VAPID) | 3 |
| Cloudflare KV + CI/CD | 3 |
| Sentry | 4 |
| BetterStack | 9 |
| Analytics | 3 |
| Meta Pixel + CAPI | 4 |
| Reporting | 2 |
| AiSensy | 2 |
| Payload | 2 |
| App Config | 3 |
| **Total** | **56** |

## Platform Injection

| Platform | Where to set | Applied to |
| --- | --- | --- |
| **Cloudflare Workers** | `wrangler secret` / Workers & Pages → Settings → Variables | `apps/web` + `apps/admin` (edge) |
| **Render** | Service → Environment tab | `apps/cms` (Payload CMS) |
| **Cloud Run** | Service → Variables & Secrets | `apps/invoicing` (PDF render) |

All platforms inject vars at runtime — no `.env` file is needed or present in production.

## GitHub Secrets

Set under **Repository Settings → Secrets and variables → Actions**:

<Tree>
  <Tree.File name="DATABASE_URL_DEV / TEST / PPRD / PROD" />
  <Tree.File name="DATABASE_URL_UNPOOLED_DEV / TEST / PPRD / PROD" />
  <Tree.File name="BETTER_AUTH_SECRET" />
  <Tree.File name="GOOGLE_OAUTH_CLIENT_ID / SECRET" />
  <Tree.File name="... (all other secrets)" />
</Tree>

Reference in GitHub Actions:

```yaml
env:
  DATABASE_URL: ${{ secrets.DATABASE_URL_PROD }}
  BETTER_AUTH_SECRET: ${{ secrets.BETTER_AUTH_SECRET }}
```
